const USE_MOCK_DATA_FOR_REPORTS = true
const MOCK_DELAY = 1000
const API_URL = import.meta.env.BASE_URL || 'https://api.example.com'

export const CONFIG = {
  USE_MOCK_DATA_FOR_REPORTS,
  API_URL,
  MOCK_DELAY
}
